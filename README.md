# task7
Creating Views
Objective:
A view is like a saved SQL query that behaves like a virtual table. You can query it like a normal table, but it doesn’t store data itself — it stores the query definition.

Tools:
**MySQL Workbench (for MySQL)**
**github**

task7.sql shows that 
table is created and sample data are inserted.
views are created.
views created for abstraction.
views created for security.

outcome:
views are created for abstraction and security.
